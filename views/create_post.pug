extends index

append header 
    title Создать пост
    script(src="https://cdnjs.cloudflare.com/ajax/libs/ace/1.31.1/ace.js")
    style 
        include css/create_post.css

append body 
    script 
        include js/form.js
        include js/create_post.js

append main 
    h1.page-header
    form(name=form.meta.name)

    script(type="text/x-ejs-template", template="post-body-editor")
        .text-unit
            textarea.styled-scroll(placeholder="Ваша новость")
            a.light.button.preview(href="#", style="display:block;margin-top:5px;width:max-content;") Предпросмотр

    script(type="text/x-ejs-template", template="post-body-preview")
        .post-body.block.styled-scroll <%- contentCompiler.compile(body) %>