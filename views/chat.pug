extends index

include units

append header
    script(src="https://unpkg.com/peerjs@1.5.1/dist/peerjs.min.js")
    style 
        include css/chat.css
        include css/content.css
append body 
    script 
        include js/chat.js

block main
    .chat-header.block 
        .info(onclick='alert("options")')
            img.avatar
            span.chat-name.finite
            span.subinfo.finite
        .actions 
            button.join-call Звонить
            button.leave-call Отключиться

    .voice-chat.block
        .preview
            span.list.finite В звонке:
            span.count (
                b.num 0
                | 
                span участников
                |)
        .voice-controls
            .personal
                span Ваш микрофон
                button.mute Включен
            .wrapper.styled-scroll
                ul.members

    .holder-wrapper.block.styled-scroll
        .load-zone
        .holder 
    .input-bar
        input.entry(type="text", placeholder="Сообщение")
        button.button#stickers 😀 
        button.button#file 📁
        button.button#send 📨
    
    script(type="text/x-ejs-template", template="stickerscw")
        .page#emoji
            .sticker-grid
                    <% emojiList.forEach(emoji => { %>
                    span.emoji <%=emoji%>
                    <% }) %>
        
        <% packs.forEach(pack => { %>
        .page.disabled(id!="<%=pack.name%>")
            .sticker-grid
                <% for(let i = 0; i < pack.count; i++) { %>
                img.sticker(src!="/public/stickers/<%=pack.name%>/<%=i%>.png")
                <% } %>
        <% }) %>

        .navigation
            button.button#emoji 😀
            <% packs.forEach(pack => { %>
            button.button(id!="<%=pack.name%>") <%=pack.name%>
            <% }) %>